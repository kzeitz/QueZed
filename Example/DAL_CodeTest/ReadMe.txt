During our first talk when we discussed the code review, we talked as if it would be a database application.
Even though later, when I received the email, it said to use a text file for storage.
Since I had already done most of the work, I decided to keep it a database application event though a database is 
certainly 'over-kill' for the task.

1. Publish the Demo db to a convenient SQL Server instance.
2. Update DemoForms.exe App.config with the appropriate SQL connection string.
3. Build and go.
4. Pre-built Debug version provided